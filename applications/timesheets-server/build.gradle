dependencies {
    compile(project(":components:timesheets"))
}

bootRun.environment([
    "WELCOME_MESSAGE"             : "hello",
    "SPRING_DATASOURCE_URL"       : timesheetsDevDb,
    "SERVER_PORT"                 : 8084,
    "REGISTRATION_SERVER_ENDPOINT": "http://registration-server"
])
